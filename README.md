# Ansible Playbook Examples
---
1. check playbook - pings all servers
2. utilities install playbook - installs utilities on all servers
3. nginx install playbook - install, enable and start nginx on servers
4. node exporter playbook - install and run node exporter for prometheus on all servers
